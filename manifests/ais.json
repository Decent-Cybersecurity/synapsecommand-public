{
  "adapter": {
    "adapter_version": "1.0.0",
    "binding": "standard-encoding",
    "capabilities": {
      "directions_exercised": [
        "ingest",
        "egress"
      ],
      "limits": {
        "absent_because": {
          "max_decompressed_bytes": "this adapter accepts no archived or compressed payload, so there is no expansion to bound",
          "max_objects": "no bound is enforced by this adapter today; the parser-safety policy's concrete bounds are owed by P5 (ARCHITECTURE.md \u00a79)",
          "max_parse_seconds": "no wall-clock bound is enforced by this adapter today; the parser-safety policy's concrete bounds are owed by P5 (ARCHITECTURE.md \u00a79)"
        },
        "declared_because": {
          "max_depth": {
            "enforced_at": "`Adapter.__init_subclass__` wraps this class's own `to_cdm` with `enforce_depth_bound` beside `enforce_input_bound` (`adapter.py`, `_bind_input_bound`): JSON text is measured off its characters by `json_nesting_depth`, decoded the way `json.loads` would decode it, and a parsed dict off its containers by `container_depth`, before `_as_parsed` or `json.loads` runs; past the bound the wrapper raises `InputTooDeep`, a `ValueError` naming both numbers.",
            "kind": "implementation_cap",
            "source": "An AIS message does not nest, but `to_cdm` also takes the parsed twin \u2014 a dict, or its JSON text \u2014 and the walk after that parse recurses once per level; no JSON document shipped under `fixtures/ais/`, goldens included, nests more than seven containers, and the deepest twin `to_cdm` is handed nests four. 64 is chosen on 2026-09-17 for the reason `tak` chose it on 2026-09-16: every walk after the parse recurses once per level, and on CPython 3.11 `json.loads` itself recurses once per container and raises `RecursionError` a little under a thousand containers deep (the reading is in `adapter.InputTooDeep`), so the bound is read off the characters before the decoder runs. Nine times the deepest JSON document shipped beside the fixtures, and every walker stays under two hundred frames. This is an IMPLEMENTATION CAP (`AIS_MAX_DEPTH`, `adapters/ais.py`) and is NOT the format's normative maximum.",
            "test": "tests/test_cdm_parser_safety.py::test_the_json_adapters_declare_a_depth_bound_and_refuse_a_document_past_it"
          },
          "max_input_bytes": {
            "enforced_at": "`Adapter.__init_subclass__` wraps this class's own `to_cdm` with `enforce_input_bound` at class-definition time (`adapter.py`, `_bind_input_bound`), so the payload is measured and refused before any decoder in this module runs",
            "kind": "implementation_cap",
            "source": "NMEA 0183 caps one sentence at 82 characters including the delimiters and the checksum (`adapters/ais.py`, `MAX_PAYLOAD_CHARS`), and the AIVDM fragment-count field is a single character, so one whole message is at most 9 x 82 = 738 octets. That figure does not cover the NMEA 0183 v4.10 TAG blocks this adapter also accepts on each line (`adapters/ais.py:79`), which is why the declared bound is not 738: 1024 carries the 738 plus a TAG block on every fragment. This is an IMPLEMENTATION CAP under M's F5.4 ruling and is NOT the format's normative maximum.",
            "test": "tests/test_cdm_input_bounds.py::test_every_adapter_refuses_one_octet_over_its_declared_bound"
          }
        },
        "max_decompressed_bytes": null,
        "max_depth": 64,
        "max_input_bytes": 1024,
        "max_objects": null,
        "max_parse_seconds": null
      },
      "message_types": [
        "AIVDM/AIVDO message types 1, 2, 3, 4, 5, 18, 19, 21"
      ],
      "unknown_fields": "preserved",
      "unknown_fields_basis": "the parsed twin is an ordinary JSON document and every key this adapter does not consume is parked verbatim under `source_extras`. The six-bit armoured payload has no room for a field nobody defined, so this declaration is about the document form and not about the wire form",
      "wire": true
    },
    "claim_external_system": null,
    "claim_status": "VERIFIED",
    "constituents": [],
    "direction": "bidirectional",
    "evidence": {
      "available": true
    },
    "format": {
      "name": "AIS (NMEA 0183 AIVDM/AIVDO)",
      "version": null
    },
    "id": "ais",
    "license_class": "LICENSED",
    "limitations": [
      "the edition this adapter is written against is NOT STATED by any document in this repository \u2014 the format has two publishers, ITU-R (M.1371) and NMEA (0183), and `fixtures/ais/spec/ais_terms.json` records both without naming the edition this implementation targets, so `format.version` is null rather than guessed",
      "message types 1, 2, 3, 4, 5, 18, 19 and 21 are in scope; every other type is named in FORMAT_COVERAGE.md with the reason it is out",
      "type 24 static data is out of scope, so a vessel's name and call sign arrive in no message this adapter reads",
      "leftovers are parked in `Entity.attributes` / `Event.payload` under `source_extras` (`lossless.residual()`), not in the origin-identifying container ARCHITECTURE.md \u00a75 gives to P3 \u2014 the Part 1 stance that section rules for the adapters already shipped",
      "the evidence RECORD for this adapter is not IN the distribution: `evidence/` is untracked and unpackaged, CI generates the set on every run and the release pipeline attaches it to the GitHub Release. `evidence.available` is true because the records for 2.1.2 are attached to the `v2.1.2` Release and retrievable by a third party, and it says nothing about what the wheel contains",
      "of \u00a73.5's five resource limits this adapter enforces ONE \u2014 `max_input_bytes`, declared in `capabilities.limits` with its basis beside it and refused before decode by the base class (round P5). The other four are still absent, each with its own reason in `capabilities.limits.absent_because`; a depth, object-count, decompression or wall-clock bound is not enforced here today"
    ],
    "limitations_empty_reason": null,
    "maturity": {
      "basis": "L3 PROVENANCE VERIFIED, from evidence that runs today. The harness's `translate`, `schema` and `provenance` checks are PASS on every fixture of this adapter, which carries L1 to L3. Its `lossless` check is PASS on every fixture as well, but that PASS rests on the value-presence heuristic and not on the path-bound ledger, because this adapter declares no `MAPPINGS` (the report's `preservation.basis` reads `heuristic`). Since 2026-09-16 its `roundtrip` column is PASS: `from_cdm(to_cdm(raw))` reproduces every byte fixture octet for octet under the declared `bytes` tolerance (`ROUNDTRIP_TOLERANCE`), the two fixtures that carry an NMEA TAG block being compared against `roundtrip_reference(raw)` \u2014 the sentences without the receiver's own annotation, which the harness names in every report, so `synapse conformance run --adapter ais` computes E = PASS and `maturity_eligible` reads above this declaration. L4 is NOT declared (ruled 2026-09-20, audit remediation F02, register \u00a74): 'applicable information survives source -> CDM -> source' is the claim the heuristic cannot prove, so the declaration stops at the last rung whose evidence is path-bound and regains L4 only when this adapter's field mappings are declared and the ledger reports no loss. The adapter's own statement of the round-trip claim is tests/test_cdm_ais_adapter.py::test_the_ingest_round_trip_is_byte_exact. L5 is likewise not declared: the rung above L4 rests on `M` (streaming) being inapplicable to every adapter in this repository, and a rung passed vacuously is not a rung declared (ARCHITECTURE.md \u00a73.6, rule 4 \u2014 the reading the ingest-only adapters apply to `E`).",
      "external_exercise": null,
      "level": "L3"
    },
    "name": "AIS",
    "payload_adapter": null,
    "profiles": [],
    "residual": "legacy"
  },
  "api": {
    "version": "3"
  },
  "cdm": {
    "schema_version": "3.0.0",
    "supported": "3.x"
  },
  "manifest_schema_version": "2.1.0",
  "schema_id": "synapse.adapter-manifest/v1"
}
