{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Decent-Cybersecurity/synapsecommand-public/security/exceptions/schema.json",
  "title": "Security exception",
  "description": "A documented, time-bounded decision not to fix a security finding immediately. SOIF Part 1 §45. This schema is HAND-WRITTEN and lives beside the files it governs rather than under `schemas/`, because `schemas/` is generated from the package's models by `python -m synapse_cdm.schemas` and a hand-written file there would be deleted by the next regeneration. An exception is not package surface: no code imports it, and the two consumers that read it (`gates/codeql_gate.py` and the pip-audit step in `.github/workflows/ci.yml`) read the directory, not the package.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "identifier",
    "affected_package",
    "version_range",
    "risk",
    "reason",
    "mitigation",
    "upstream_status",
    "owner",
    "expiry",
    "created",
    "references"
  ],
  "properties": {
    "identifier": {
      "type": "string",
      "minLength": 1,
      "description": "The finding's own id, exactly as the tool that reports it prints it: a CodeQL rule id (`py/sql-injection`), a GitHub advisory (`GHSA-xxxx-xxxx-xxxx`), a PyPA advisory (`PYSEC-2026-1`) or a CVE. This is the string the consumers match on, so it is the tool's spelling and never a paraphrase.",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._/-]*$"
    },
    "affected_package": {
      "type": "string",
      "minLength": 1,
      "description": "The distribution the finding is in, or `synapse-cdm` itself when the finding is in this repository's own code."
    },
    "version_range": {
      "type": "string",
      "minLength": 1,
      "description": "The versions this exception covers, as a PEP 440 specifier (`>=2.0,<2.2`) or the literal `*` when the finding is in this repository's own code and versions do not apply. An exception with no range is an exception with no end, which is what §45 forbids."
    },
    "risk": {
      "type": "string",
      "minLength": 30,
      "description": "What an attacker gets if this is exploited HERE, in this repository's use of the package — not the advisory's generic wording. Thirty characters is a floor against a one-word entry, not a target."
    },
    "reason": {
      "type": "string",
      "minLength": 30,
      "description": "Why the exception is being GRANTED — the decision, not its consequences. “No fix available upstream” is a reason; “low priority” is not. What holds the risk down belongs in `mitigation`, and what would end the exception belongs in `upstream_status`: M's ruling of 2026-09-08T16:45:00Z split those two out of this field, so that neither can be omitted by writing a longer sentence here."
    },
    "mitigation": {
      "type": "string",
      "minLength": 30,
      "description": "The concrete compensating controls IN FORCE NOW — what is already true of this repository that keeps the risk in `risk` from being reachable, or from mattering if it is. Present tense and checkable: a gate that runs, a path no build takes, a file the distribution does not carry. A control somebody intends to add is not a mitigation. Required since M's ruling of 2026-09-08T16:45:00Z; the thirty-character floor is the one `risk` and `reason` already carry, and it is a floor against a one-word entry rather than a target."
    },
    "upstream_status": {
      "type": "string",
      "minLength": 30,
      "description": "Whether a fix exists upstream, the state of the upstream issue or advisory, and THE EVENT THAT WILL TRIGGER REMOVAL of this exception. The trigger is what bounds the exception by something other than the calendar: `expiry` fails the suite on a date, and this field says what would end the exception before that date — which is what M's ruling of 2026-09-08 requires when it says a fix arriving before release means the exception goes and the dependency is upgraded. Required since M's ruling of 2026-09-08T16:45:00Z."
    },
    "owner": {
      "type": "string",
      "minLength": 1,
      "description": "A person or team that answers for this exception. A GitHub handle or an email address; never `unassigned`, never a role nobody holds."
    },
    "expiry": {
      "type": "string",
      "format": "date",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "description": "The date after which this exception is no longer valid, ISO 8601. `tests/test_cdm_security_exceptions.py` fails the suite the day this passes — unconditionally and with no flag — which is what makes §45's “no permanent undocumented exemptions” enforced rather than intended."
    },
    "created": {
      "type": "string",
      "format": "date",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$",
      "description": "The date this exception was written, ISO 8601. Kept so that the interval between `created` and `expiry` is readable rather than inferred."
    },
    "references": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "pattern": "^https?://"
      },
      "description": "At least one URL: the advisory, the upstream issue, the CodeQL query's documentation. An exception with no reference is a decision with no evidence behind it."
    }
  }
}
