{
  "$defs": {
    "Integrity": {
      "additionalProperties": false,
      "description": "DESIGNED, NOT IMPLEMENTED \u2014 the field the PQC signature will occupy.\n\nNo crypto happens in this package (tests/test_cdm_boundary.py asserts the package imports\nno crypto module). The field exists from day one so that turning signing on is a value\nchange rather than a schema change: a schema change would be a MAJOR bump rippling through\nevery store and every consumer, and would arrive exactly when the signing work is already\nlate.\n\n`algorithm` is a free string rather than an enum, naming what the platform's ledger\nalready uses \u2014 ML-DSA-87 for entry signatures, SLH-DSA for checkpoints. Free, because the\nalgorithm that replaces those is not knowable now, and an enum would make the migration a\nMAJOR bump for a value nobody reasons over programmatically.\n\nAll three fields or none. A block holding a signature with no algorithm is unverifiable,\nand an unverifiable signature that LOOKS present is worse than an absent one: it reads as\nassurance to everything downstream that does not check.",
      "properties": {
        "algorithm": {
          "description": "e.g. ML-DSA-87, SLH-DSA-SHAKE-256s.",
          "minLength": 1,
          "title": "Algorithm",
          "type": "string"
        },
        "chain_hash": {
          "description": "Hash binding this object to the chain.",
          "minLength": 1,
          "title": "Chain Hash",
          "type": "string"
        },
        "signature": {
          "minLength": 1,
          "title": "Signature",
          "type": "string"
        }
      },
      "required": [
        "signature",
        "algorithm",
        "chain_hash"
      ],
      "title": "Integrity",
      "type": "object"
    },
    "LineString": {
      "additionalProperties": false,
      "properties": {
        "coordinates": {
          "items": {
            "items": {
              "type": "number"
            },
            "type": "array"
          },
          "minItems": 2,
          "title": "Coordinates",
          "type": "array"
        },
        "type": {
          "const": "LineString",
          "default": "LineString",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "coordinates"
      ],
      "title": "LineString",
      "type": "object"
    },
    "ObjectType": {
      "description": "What we push OUT \u2014 the egress direction, e.g. to TAK as a drawing object.",
      "enum": [
        "COA_SKETCH",
        "ROUTE",
        "CONTROL_MEASURE",
        "ANNOTATION"
      ],
      "title": "ObjectType",
      "type": "string"
    },
    "Point": {
      "additionalProperties": false,
      "properties": {
        "coordinates": {
          "items": {
            "type": "number"
          },
          "title": "Coordinates",
          "type": "array"
        },
        "type": {
          "const": "Point",
          "default": "Point",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "coordinates"
      ],
      "title": "Point",
      "type": "object"
    },
    "Polygon": {
      "additionalProperties": false,
      "properties": {
        "coordinates": {
          "items": {
            "items": {
              "items": {
                "type": "number"
              },
              "type": "array"
            },
            "type": "array"
          },
          "minItems": 1,
          "title": "Coordinates",
          "type": "array"
        },
        "type": {
          "const": "Polygon",
          "default": "Polygon",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "coordinates"
      ],
      "title": "Polygon",
      "type": "object"
    },
    "SourceId": {
      "additionalProperties": false,
      "description": "One external identifier for an object \u2014 the provenance mapping.\n\nA list of these, not one, because the same object arrives from several systems: the same\nvessel is an MMSI to AIS, a track number to STANAG 4676 and a UID to TAK. Fusion joins\nthem later; the adapter's job is to record which name its own system used, and never to\noverwrite another system's entry.",
      "properties": {
        "external_id": {
          "description": "That system's own identifier.",
          "minLength": 1,
          "title": "External Id",
          "type": "string"
        },
        "system": {
          "description": "The external system, e.g. PNTMAP, TAK.",
          "minLength": 1,
          "title": "System",
          "type": "string"
        }
      },
      "required": [
        "system",
        "external_id"
      ],
      "title": "SourceId",
      "type": "object"
    },
    "SourceRef": {
      "additionalProperties": false,
      "description": "Which adapter produced this object, from which system, and whether it is real.\n\n`synthetic` is required and has no default. Every fixture in this repository is synthetic\nand every scenario package is too (TR-12), and the platform keeps the synthetic and live\nlayers apart over one interface \u2014 so an object that does not say which layer it belongs to\ncannot be filed. A default of `false` would silently promote exercise data to operational\ndata, which is the dangerous direction; a default of `true` would silently demote live\ndata and hide it from an operator. There is no safe default, so there is no default.",
      "properties": {
        "adapter": {
          "description": "Adapter name, e.g. pntmap.",
          "minLength": 1,
          "title": "Adapter",
          "type": "string"
        },
        "adapter_version": {
          "description": "Adapter semver.",
          "minLength": 1,
          "title": "Adapter Version",
          "type": "string"
        },
        "synthetic": {
          "description": "true for anything not from a real source (TR-12).",
          "title": "Synthetic",
          "type": "boolean"
        },
        "system": {
          "description": "The external system this came from.",
          "minLength": 1,
          "title": "System",
          "type": "string"
        }
      },
      "required": [
        "system",
        "adapter",
        "adapter_version",
        "synthetic"
      ],
      "title": "SourceRef",
      "type": "object"
    }
  },
  "$id": "https://synapsecommand.local/cdm/1.0.0/plan_object.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "What we push OUT: a drawing a commander's plan puts on someone else's map.\n\nGeometry is REQUIRED here, unlike on Event. An overlay with no geometry cannot be drawn,\nso an egress adapter would have to either invent a location or silently drop the object \u2014\nand a COA sketch that quietly fails to appear on the TAK client is the worst of the three\noutcomes, because everyone assumes it arrived.",
  "properties": {
    "expires_at": {
      "anyOf": [
        {
          "description": "RFC 3339 UTC, exactly three decimal places, always Z.",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "When the drawing should disappear. None = until explicitly removed \u2014 which for a stale COA sketch on a live map is a decision, so state it.",
      "title": "Expires At"
    },
    "geometry": {
      "description": "GeoJSON, WGS84, [lon, lat] order. Required.",
      "discriminator": {
        "mapping": {
          "LineString": "#/$defs/LineString",
          "Point": "#/$defs/Point",
          "Polygon": "#/$defs/Polygon"
        },
        "propertyName": "type"
      },
      "oneOf": [
        {
          "$ref": "#/$defs/Point"
        },
        {
          "$ref": "#/$defs/LineString"
        },
        {
          "$ref": "#/$defs/Polygon"
        }
      ],
      "title": "Geometry"
    },
    "integrity": {
      "anyOf": [
        {
          "$ref": "#/$defs/Integrity"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "PQC signature block \u2014 designed, not yet populated."
    },
    "label": {
      "anyOf": [
        {
          "minLength": 1,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "What a client shows next to the drawing. None = unlabelled; never an empty string, which renders as a blank callout.",
      "title": "Label"
    },
    "object_id": {
      "format": "uuid",
      "title": "Object Id",
      "type": "string"
    },
    "object_kind": {
      "const": "plan_object",
      "default": "plan_object",
      "title": "Object Kind",
      "type": "string"
    },
    "object_type": {
      "$ref": "#/$defs/ObjectType"
    },
    "schema_version": {
      "default": "1.0.0",
      "description": "Semver of the CDM this object was written against.",
      "title": "Schema Version",
      "type": "string"
    },
    "source": {
      "$ref": "#/$defs/SourceRef",
      "description": "Which adapter produced this object. Required on every kind."
    },
    "source_ids": {
      "description": "Every external identifier this object is known by. At least one, on EVERY kind \u2014 see the class docstring.",
      "items": {
        "$ref": "#/$defs/SourceId"
      },
      "minItems": 1,
      "title": "Source Ids",
      "type": "array"
    },
    "style": {
      "additionalProperties": true,
      "description": "Rendering HINTS, not requirements \u2014 stroke, fill, opacity, dash. A receiving client is free to ignore them, so nothing that changes MEANING may live here (an affiliation belongs on the entity, not in a colour).",
      "title": "Style",
      "type": "object"
    }
  },
  "required": [
    "source",
    "source_ids",
    "object_id",
    "object_type",
    "geometry"
  ],
  "title": "PlanObject",
  "type": "object",
  "x-cdm-schema-version": "1.0.0"
}
