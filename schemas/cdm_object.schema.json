{
  "$defs": {
    "Affiliation": {
      "description": "Maps to MIL-STD-2525 standard identity \u2014 see models.standard_identity().\n\nFour members, not 2525's seven: PENDING, ASSUMED_FRIEND and SUSPECT are judgements a\nfusion layer makes, not facts an adapter can read off a wire format. An adapter that\ninvented ASSUMED_FRIEND would be doing business logic, which adapters may not do.\nThe source's own wording is preserved in `attributes` when it is finer than this.",
      "enum": [
        "FRIENDLY",
        "HOSTILE",
        "NEUTRAL",
        "UNKNOWN"
      ],
      "title": "Affiliation",
      "type": "string"
    },
    "Entity": {
      "additionalProperties": false,
      "description": "Anything that exists on the map, at a stated time, with stated confidence.",
      "properties": {
        "affiliation": {
          "$ref": "#/$defs/Affiliation"
        },
        "attributes": {
          "additionalProperties": true,
          "description": "Source-specific fields the CDM has no home for. The never-drop bag: park data here rather than discarding it.",
          "title": "Attributes",
          "type": "object"
        },
        "confidence": {
          "anyOf": [
            {
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "0..1. None = unknown; 0 means certainty-that-not, which is a claim.",
          "title": "Confidence"
        },
        "entity_id": {
          "description": "Stable across updates \u2014 derived, see ids.derive(). Never drawn at random.",
          "format": "uuid",
          "title": "Entity Id",
          "type": "string"
        },
        "entity_type": {
          "$ref": "#/$defs/EntityType"
        },
        "integrity": {
          "anyOf": [
            {
              "$ref": "#/$defs/Integrity"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "PQC signature block \u2014 designed, not yet populated."
        },
        "kinematics": {
          "anyOf": [
            {
              "$ref": "#/$defs/Kinematics"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "object_kind": {
          "const": "entity",
          "default": "entity",
          "title": "Object Kind",
          "type": "string"
        },
        "ontology_types": {
          "description": "Optional SC-OES semantic types \u2014 absolute ontology identifiers saying what this thing IS in operational terms. Empty = the producer asserted none. Never derived from entity_type, and entity_type is never derived from it.",
          "items": {
            "type": "string"
          },
          "title": "Ontology Types",
          "type": "array"
        },
        "position": {
          "anyOf": [
            {
              "$ref": "#/$defs/Position"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "None = position unknown. NEVER a Position holding zeros."
        },
        "schema_version": {
          "default": "2.0.0",
          "description": "Semver of the CDM this object was written against.",
          "title": "Schema Version",
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/SourceRef",
          "description": "Which adapter produced this object. Required on every kind."
        },
        "source_ids": {
          "description": "Every external identifier this object is known by. At least one, on EVERY kind \u2014 see the class docstring.",
          "items": {
            "$ref": "#/$defs/SourceId"
          },
          "minItems": 1,
          "title": "Source Ids",
          "type": "array"
        },
        "symbol": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "MIL-STD-2525D SIDC, 20 digits. None when the source states no symbol \u2014 see symbology.sidc_from_affiliation() for deriving one.",
          "title": "Symbol"
        },
        "valid_from": {
          "description": "When this state began.",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
          "title": "Valid From",
          "type": "string"
        },
        "valid_to": {
          "anyOf": [
            {
              "description": "RFC 3339 UTC, exactly three decimal places, always Z.",
              "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When it ceased. None = still current / open-ended.",
          "title": "Valid To"
        }
      },
      "required": [
        "source",
        "source_ids",
        "entity_id",
        "entity_type",
        "affiliation",
        "valid_from"
      ],
      "title": "Entity",
      "type": "object"
    },
    "EntityRelation": {
      "additionalProperties": false,
      "description": "What ROLE an entity already related to this event plays in it.\n\n`Event.related_entities` remains the single answer to \"which entities does this event\nconcern\"; this adds the role. The membership rule that keeps the two lists agreeing lives on\n`Event` (`models.py`), because it is the only place both lists are visible.",
      "properties": {
        "entity_id": {
          "description": "Must also appear in the event's related_entities \u2014 see Event._oes_entities.",
          "format": "uuid",
          "title": "Entity Id",
          "type": "string"
        },
        "predicate": {
          "description": "An absolute semantic identifier: a governed ontology term or a valid third-party one. A bare word or a local name is refused.",
          "minLength": 1,
          "title": "Predicate",
          "type": "string"
        }
      },
      "required": [
        "entity_id",
        "predicate"
      ],
      "title": "EntityRelation",
      "type": "object"
    },
    "EntityType": {
      "enum": [
        "UNIT",
        "PLATFORM",
        "SENSOR",
        "FACILITY",
        "EVACUEE_GROUP",
        "INTERFERENCE_SOURCE",
        "OVERLAY_OBJECT",
        "UNKNOWN"
      ],
      "title": "EntityType",
      "type": "string"
    },
    "Event": {
      "additionalProperties": false,
      "description": "Anything that happens. The audit-bearing object: two timestamps and a source, always.",
      "properties": {
        "event_id": {
          "format": "uuid",
          "title": "Event Id",
          "type": "string"
        },
        "event_type": {
          "$ref": "#/$defs/EventType"
        },
        "geometry": {
          "anyOf": [
            {
              "discriminator": {
                "mapping": {
                  "LineString": "#/$defs/LineString",
                  "Point": "#/$defs/Point",
                  "Polygon": "#/$defs/Polygon"
                },
                "propertyName": "type"
              },
              "oneOf": [
                {
                  "$ref": "#/$defs/Point"
                },
                {
                  "$ref": "#/$defs/LineString"
                },
                {
                  "$ref": "#/$defs/Polygon"
                }
              ]
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "GeoJSON, WGS84, [lon, lat] order \u2014 e.g. a jamming footprint.",
          "title": "Geometry"
        },
        "integrity": {
          "anyOf": [
            {
              "$ref": "#/$defs/Integrity"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "PQC signature block \u2014 designed, not yet populated."
        },
        "object_kind": {
          "const": "event",
          "default": "event",
          "title": "Object Kind",
          "type": "string"
        },
        "observed_at": {
          "description": "When the SOURCE saw it. Never receipt time.",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
          "title": "Observed At",
          "type": "string"
        },
        "oes": {
          "anyOf": [
            {
              "$ref": "#/$defs/OesMetadata"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "The SC-OES wire-semantic block. None = the producer made no SC-OES assertion, which is NOT the producer asserting defaults. Optional so that every object written before SC-OES existed stays structurally valid and every producer that knows nothing about SC-OES keeps emitting objects these models accept."
        },
        "payload": {
          "additionalProperties": true,
          "description": "Event-specific fields. Validated against PAYLOAD_MODELS[event_type] when one is registered; free-form otherwise. Also the never-drop bag for events.",
          "title": "Payload",
          "type": "object"
        },
        "received_at": {
          "description": "When WE took delivery. Never source time.",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
          "title": "Received At",
          "type": "string"
        },
        "related_entities": {
          "description": "entity_id values this event concerns. Empty when the event concerns no specific entity (a feed-level status change).",
          "items": {
            "format": "uuid",
            "type": "string"
          },
          "title": "Related Entities",
          "type": "array"
        },
        "schema_version": {
          "default": "2.0.0",
          "description": "Semver of the CDM this object was written against.",
          "title": "Schema Version",
          "type": "string"
        },
        "severity": {
          "$ref": "#/$defs/Severity"
        },
        "source": {
          "$ref": "#/$defs/SourceRef",
          "description": "Which adapter produced this object. Required on every kind."
        },
        "source_ids": {
          "description": "Every external identifier this object is known by. At least one, on EVERY kind \u2014 see the class docstring.",
          "items": {
            "$ref": "#/$defs/SourceId"
          },
          "minItems": 1,
          "title": "Source Ids",
          "type": "array"
        }
      },
      "required": [
        "source",
        "source_ids",
        "event_id",
        "event_type",
        "severity",
        "observed_at",
        "received_at"
      ],
      "title": "Event",
      "type": "object"
    },
    "EventClass": {
      "description": "What KIND of assertion an event is \u2014 the coarsest semantic distinction SC-OES makes.\n\nEight members, and a consumer that understands no event type at all can still act on this\none. It is asserted by the producer and never inferred: `02-event-classes.md` forbids\nderiving it from `type_id`, from the payload or from the producer's identity, because the\nwhole value of the field is that somebody took responsibility for the claim.",
      "enum": [
        "OBSERVATION",
        "STATE_CHANGE",
        "CONSTRAINT",
        "ASSESSMENT",
        "IMPACT",
        "RECOMMENDATION",
        "DECISION",
        "ACTION"
      ],
      "title": "EventClass",
      "type": "string"
    },
    "EventRelation": {
      "additionalProperties": false,
      "description": "One typed reference from this event to another event.\n\nBoth halves are required: a predicate with no target says nothing, and a target with no\npredicate is the association `CORRELATES_WITH` exists to spell honestly.",
      "properties": {
        "event_id": {
          "description": "The event this relation points at. It need not be locally available \u2014 rejecting an event because its antecedent has not arrived would make delivery order part of the contract.",
          "format": "uuid",
          "title": "Event Id",
          "type": "string"
        },
        "predicate": {
          "$ref": "#/$defs/EventRelationPredicate",
          "description": "One of the seven governed predicates. An unrecognised predicate is refused rather than read as a nearby one."
        }
      },
      "required": [
        "predicate",
        "event_id"
      ],
      "title": "EventRelation",
      "type": "object"
    },
    "EventRelationPredicate": {
      "description": "The seven governed relationship predicates. Direction is part of the meaning.\n\nFive are carried by the LATER event and point at the earlier one; `CONTRADICTS` and\n`CORRELATES_WITH` are conceptually symmetric. An unrecognised predicate is a failure and\nmust not be interpreted as a nearby one \u2014 `08-event-relationships.md` is explicit, because\nthe nearby predicate is always a stronger claim than the one the producer could support.",
      "enum": [
        "DERIVED_FROM",
        "UPDATES",
        "SUPERSEDES",
        "RESOLVES",
        "RETRACTS",
        "CONTRADICTS",
        "CORRELATES_WITH"
      ],
      "title": "EventRelationPredicate",
      "type": "string"
    },
    "EventType": {
      "enum": [
        "DETECTION",
        "GNSS_INTERFERENCE",
        "TRACK_UPDATE",
        "ALERT",
        "STATUS_CHANGE",
        "PLAN_INJECT",
        "SIM_RESULT"
      ],
      "title": "EventType",
      "type": "string"
    },
    "EvidenceKind": {
      "description": "What an evidence reference points at: another assertion, a source record, or the outside.",
      "enum": [
        "EVENT",
        "SOURCE_RECORD",
        "EXTERNAL_ARTIFACT"
      ],
      "title": "EvidenceKind",
      "type": "string"
    },
    "EvidenceRef": {
      "additionalProperties": false,
      "description": "What stands behind an assertion. Descriptive: nothing here is fetched or verified.\n\nOne model with a declared `kind` rather than three, because `evidence[]` is a heterogeneous\nlist and a discriminated union in the wire form would make a non-Python consumer negotiate a\ndiscriminator to read a citation. The per-kind field rules are a validator instead, so the\npublished schema stays readable and the refusal names the kind and the field.",
      "properties": {
        "description": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "event_id": {
          "anyOf": [
            {
              "format": "uuid",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "EVENT only. The cited event need not resolve locally, and a consumer that cannot find it must not reject the citing event for that reason.",
          "title": "Event Id"
        },
        "hash": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "DESCRIPTIVE METADATA ONLY. Not an integrity guarantee, not a signature and not evidence of authenticity; no conformance dimension asserts anything about its value. SC-OES v0.1.0 implements no signing and no verification.",
          "title": "Hash"
        },
        "kind": {
          "$ref": "#/$defs/EvidenceKind",
          "description": "Which of the three kinds of reference this is."
        },
        "media_type": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Media Type"
        },
        "source_id": {
          "anyOf": [
            {
              "$ref": "#/$defs/SourceId"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "SOURCE_RECORD only. The CDM's own source-identifier representation, reused rather than re-invented \u2014 never the pair flattened into one opaque string."
        },
        "uri": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "EXTERNAL_ARTIFACT only. Never retrieved, at validation time or any other.",
          "title": "Uri"
        }
      },
      "required": [
        "kind"
      ],
      "title": "EvidenceRef",
      "type": "object"
    },
    "Integrity": {
      "additionalProperties": false,
      "description": "DESIGNED, NOT IMPLEMENTED \u2014 the field the PQC signature will occupy.\n\nNo crypto happens in this package (tests/test_cdm_boundary.py asserts the package imports\nno crypto module). The field exists from day one so that turning signing on is a value\nchange rather than a schema change: a schema change would be a MAJOR bump rippling through\nevery store and every consumer, and would arrive exactly when the signing work is already\nlate.\n\n`algorithm` is a free string rather than an enum, naming what the platform's ledger\nalready uses \u2014 ML-DSA-87 for entry signatures, SLH-DSA for checkpoints. Free, because the\nalgorithm that replaces those is not knowable now, and an enum would make the migration a\nMAJOR bump for a value nobody reasons over programmatically.\n\nAll three fields or none. A block holding a signature with no algorithm is unverifiable,\nand an unverifiable signature that LOOKS present is worse than an absent one: it reads as\nassurance to everything downstream that does not check.",
      "properties": {
        "algorithm": {
          "description": "e.g. ML-DSA-87, SLH-DSA-SHAKE-256s.",
          "minLength": 1,
          "title": "Algorithm",
          "type": "string"
        },
        "chain_hash": {
          "description": "Hash binding this object to the chain.",
          "minLength": 1,
          "title": "Chain Hash",
          "type": "string"
        },
        "signature": {
          "minLength": 1,
          "title": "Signature",
          "type": "string"
        }
      },
      "required": [
        "signature",
        "algorithm",
        "chain_hash"
      ],
      "title": "Integrity",
      "type": "object"
    },
    "Kinematics": {
      "additionalProperties": false,
      "description": "Motion. Every field optional, and absent means UNKNOWN, never zero.\n\nThis is the AIS sentinel lesson in schema form: 0 kt is measured stillness, 0 deg is a\ncourse due north, 0 m/s climb is level flight. All three are real measurements, so none of\nthem can double as \"no data\" \u2014 the adapter translates the source's sentinel to None.",
      "properties": {
        "climb_mps": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Metres per second, negative = descending.",
          "title": "Climb Mps"
        },
        "course_deg": {
          "anyOf": [
            {
              "exclusiveMaximum": 360.0,
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Degrees true, [0, 360).",
          "title": "Course Deg"
        },
        "speed_mps": {
          "anyOf": [
            {
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Metres per second.",
          "title": "Speed Mps"
        }
      },
      "title": "Kinematics",
      "type": "object"
    },
    "LifecycleStatus": {
      "description": "The lifecycle state of the represented CONDITION \u2014 not of the message, not of a workflow.\n\nOptional, and absence is the ordinary case: most sensor sources report occurrences rather\nthan managed conditions. `05-lifecycle.md` forbids defaulting it to `ACTIVE` or to anything\nelse, and forbids deriving it from the effective interval \u2014 an event whose `effective_to`\nhas passed is not thereby `EXPIRED`, because the interval is what the producer said about\nthe world and this is what the producer says about the assertion's own standing.",
      "enum": [
        "PLANNED",
        "ACTIVE",
        "RESOLVED",
        "EXPIRED",
        "CANCELLED",
        "RETRACTED",
        "SUPERSEDED"
      ],
      "title": "LifecycleStatus",
      "type": "string"
    },
    "LineString": {
      "additionalProperties": false,
      "properties": {
        "coordinates": {
          "items": {
            "items": {
              "type": "number"
            },
            "type": "array"
          },
          "minItems": 2,
          "title": "Coordinates",
          "type": "array"
        },
        "type": {
          "const": "LineString",
          "default": "LineString",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "coordinates"
      ],
      "title": "LineString",
      "type": "object"
    },
    "ObjectType": {
      "description": "What we push OUT \u2014 the egress direction, e.g. to TAK as a drawing object.",
      "enum": [
        "COA_SKETCH",
        "ROUTE",
        "CONTROL_MEASURE",
        "ANNOTATION"
      ],
      "title": "ObjectType",
      "type": "string"
    },
    "OesMetadata": {
      "additionalProperties": false,
      "description": "The wire-semantic block. \u00a752's thirteen fields, and `maturity` is not among them.\n\nMaturity is a property of a governed semantic DEFINITION, not of an occurrence: it lives in\nthe event registry, in ontology-term metadata and in profile documents. Putting it on the\nwire would duplicate registry governance metadata on every message and give it a second\nauthority, which would then go stale (`12-versioning.md`).\n\nThree states are distinguished throughout and are not collapsed (`01-core.md`): a field\npresent with a value is ASSERTED, an absent field or a null `confidence` is UNKNOWN, and a\nvalue whose meaning is \"none\" is ASSERTED-ABSENT. An unknown value is never represented as a\nzero, an empty string or a default enumeration member \u2014 which is why every optional field\nhere defaults to `None` and none of the enumerations carries an UNKNOWN member the way the\nCDM's own vocabularies do (`enums.py:3`). The CDM's rule is right for a closed structural\nclassification a map has to render; SC-OES's optional fields are assertions a producer either\nmade or did not, and \"not asserted\" is exactly what absence already says.",
      "properties": {
        "confidence": {
          "anyOf": [
            {
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "0..1 on the producer's own scale. None = unknown, never 0.0, and this specification defines no universal confidence algorithm.",
          "title": "Confidence"
        },
        "effective_from": {
          "anyOf": [
            {
              "description": "RFC 3339 UTC, exactly three decimal places, always Z.",
              "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When the represented condition begins. NEVER defaulted to observed_at, received_at or the time of validation.",
          "title": "Effective From"
        },
        "effective_to": {
          "anyOf": [
            {
              "description": "RFC 3339 UTC, exactly three decimal places, always Z.",
              "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When it ceases. None = the producer did not state an end; it does not mean the condition is permanent and it does not mean it is still in force.",
          "title": "Effective To"
        },
        "entity_relations": {
          "description": "Roles for entities already in related_entities.",
          "items": {
            "$ref": "#/$defs/EntityRelation"
          },
          "title": "Entity Relations",
          "type": "array"
        },
        "event_class": {
          "$ref": "#/$defs/EventClass",
          "description": "Asserted by the producer. Never inferred from type_id or from the payload."
        },
        "event_relations": {
          "description": "Empty or absent asserts nothing. No universal cap.",
          "items": {
            "$ref": "#/$defs/EventRelation"
          },
          "title": "Event Relations",
          "type": "array"
        },
        "evidence": {
          "description": "What stands behind the assertion. No universal cap.",
          "items": {
            "$ref": "#/$defs/EvidenceRef"
          },
          "title": "Evidence",
          "type": "array"
        },
        "extensions": {
          "additionalProperties": true,
          "description": "The one declared open bag. Keys are x.<namespace>.<name>; sc.* is reserved and rejected in v0.1; values are preserved and never interpreted.",
          "title": "Extensions",
          "type": "object"
        },
        "security": {
          "anyOf": [
            {
              "$ref": "#/$defs/SecurityMarking"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Transported markings. Absence is never a conformance failure and is never proof of unclassified status."
        },
        "spec_version": {
          "description": "The SC-OES version whose semantics the producer is claiming. Semver, on the same rule CDMBase applies to schema_version; NOT derived from SCHEMA_VERSION or PACKAGE_VERSION, and not required to equal this package's SC_OES_VERSION \u2014 a producer at a later spec version stays transportable.",
          "title": "Spec Version",
          "type": "string"
        },
        "status": {
          "anyOf": [
            {
              "$ref": "#/$defs/LifecycleStatus"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "None = the source says nothing about lifecycle."
        },
        "type_id": {
          "description": "The governed or third-party semantic type identifier, under one of the two frozen grammars. Syntax here; recognition is dimension C's.",
          "title": "Type Id",
          "type": "string"
        },
        "verification": {
          "anyOf": [
            {
              "$ref": "#/$defs/Verification"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "None = nothing is asserted about corroboration."
        }
      },
      "required": [
        "spec_version",
        "event_class",
        "type_id"
      ],
      "title": "OesMetadata",
      "type": "object"
    },
    "PlanObject": {
      "additionalProperties": false,
      "description": "What we push OUT: a drawing a commander's plan puts on someone else's map.\n\nGeometry is REQUIRED here, unlike on Event. An overlay with no geometry cannot be drawn,\nso an egress adapter would have to either invent a location or silently drop the object \u2014\nand a COA sketch that quietly fails to appear on the TAK client is the worst of the three\noutcomes, because everyone assumes it arrived.",
      "properties": {
        "expires_at": {
          "anyOf": [
            {
              "description": "RFC 3339 UTC, exactly three decimal places, always Z.",
              "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When the drawing should disappear. None = until explicitly removed \u2014 which for a stale COA sketch on a live map is a decision, so state it.",
          "title": "Expires At"
        },
        "geometry": {
          "description": "GeoJSON, WGS84, [lon, lat] order. Required.",
          "discriminator": {
            "mapping": {
              "LineString": "#/$defs/LineString",
              "Point": "#/$defs/Point",
              "Polygon": "#/$defs/Polygon"
            },
            "propertyName": "type"
          },
          "oneOf": [
            {
              "$ref": "#/$defs/Point"
            },
            {
              "$ref": "#/$defs/LineString"
            },
            {
              "$ref": "#/$defs/Polygon"
            }
          ],
          "title": "Geometry"
        },
        "integrity": {
          "anyOf": [
            {
              "$ref": "#/$defs/Integrity"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "PQC signature block \u2014 designed, not yet populated."
        },
        "label": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What a client shows next to the drawing. None = unlabelled; never an empty string, which renders as a blank callout.",
          "title": "Label"
        },
        "object_id": {
          "format": "uuid",
          "title": "Object Id",
          "type": "string"
        },
        "object_kind": {
          "const": "plan_object",
          "default": "plan_object",
          "title": "Object Kind",
          "type": "string"
        },
        "object_type": {
          "$ref": "#/$defs/ObjectType"
        },
        "schema_version": {
          "default": "2.0.0",
          "description": "Semver of the CDM this object was written against.",
          "title": "Schema Version",
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/SourceRef",
          "description": "Which adapter produced this object. Required on every kind."
        },
        "source_ids": {
          "description": "Every external identifier this object is known by. At least one, on EVERY kind \u2014 see the class docstring.",
          "items": {
            "$ref": "#/$defs/SourceId"
          },
          "minItems": 1,
          "title": "Source Ids",
          "type": "array"
        },
        "style": {
          "additionalProperties": true,
          "description": "Rendering HINTS, not requirements \u2014 stroke, fill, opacity, dash. A receiving client is free to ignore them, so nothing that changes MEANING may live here (an affiliation belongs on the entity, not in a colour).",
          "title": "Style",
          "type": "object"
        }
      },
      "required": [
        "source",
        "source_ids",
        "object_id",
        "object_type",
        "geometry"
      ],
      "title": "PlanObject",
      "type": "object"
    },
    "Point": {
      "additionalProperties": false,
      "properties": {
        "coordinates": {
          "items": {
            "type": "number"
          },
          "title": "Coordinates",
          "type": "array"
        },
        "type": {
          "const": "Point",
          "default": "Point",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "coordinates"
      ],
      "title": "Point",
      "type": "object"
    },
    "Polygon": {
      "additionalProperties": false,
      "properties": {
        "coordinates": {
          "items": {
            "items": {
              "items": {
                "type": "number"
              },
              "type": "array"
            },
            "type": "array"
          },
          "minItems": 1,
          "title": "Coordinates",
          "type": "array"
        },
        "type": {
          "const": "Polygon",
          "default": "Polygon",
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "coordinates"
      ],
      "title": "Polygon",
      "type": "object"
    },
    "Position": {
      "additionalProperties": false,
      "description": "A fix. Both coordinates required \u2014 that is how the null-never-zero rule is structural.\n\nAn unknown position is the ABSENCE of this object (`entity.position is None`), never a\nPosition holding zeros. Because lat and lon are required here, an adapter cannot express\n\"unknown\" as (0, 0) even by accident: it has to either omit the Position or state a real\ncoordinate. Coordinate zero is a real point in the Gulf of Guinea, and a contact painted\nthere is a contact that does not exist.\n\n`accuracy_m` absent means unknown accuracy, NOT perfect accuracy. Zero would mean a fix\nwith no error, which no sensor produces.",
      "properties": {
        "accuracy_m": {
          "anyOf": [
            {
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Metres, 1-sigma. None = unknown, never 0.",
          "title": "Accuracy M"
        },
        "alt_m": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Metres HAE. None = unknown.",
          "title": "Alt M"
        },
        "lat": {
          "description": "WGS84 decimal degrees.",
          "maximum": 90.0,
          "minimum": -90.0,
          "title": "Lat",
          "type": "number"
        },
        "lon": {
          "description": "WGS84 decimal degrees.",
          "maximum": 180.0,
          "minimum": -180.0,
          "title": "Lon",
          "type": "number"
        },
        "position_source": {
          "$ref": "#/$defs/PositionSource",
          "description": "How the fix was obtained \u2014 the field that survives GNSS denial."
        }
      },
      "required": [
        "lat",
        "lon",
        "position_source"
      ],
      "title": "Position",
      "type": "object"
    },
    "PositionSource": {
      "description": "How the position was obtained. Load-bearing in a GNSS-denied environment.\n\nThis is the field that lets a commander tell a fix from a guess. When PNTMAP reports\njamming over an area, every GNSS-sourced position inside that area becomes suspect and\nevery INERTIAL or MANUAL one does not \u2014 a distinction that is impossible to make after\nthe fact if the adapter flattened them all to \"position\".",
      "enum": [
        "GNSS",
        "INERTIAL",
        "MANUAL",
        "ESTIMATED"
      ],
      "title": "PositionSource",
      "type": "string"
    },
    "SecurityMarking": {
      "additionalProperties": false,
      "description": "Handling markings, TRANSPORTED. SC-OES does not interpret or enforce them.\n\nThree propositions `10-security-markings.md` states because each is routinely assumed away:\npresence of a marking is not authorization, absence is not proof of unclassified status, and\ntransport is not enforcement. SC-OES is not a cross-domain guard and must not be represented\nas one; nothing in this package could enforce a marking, and a deployment that enforces one\ndoes so with its own accredited mechanism, which knows the scheme and is answerable for the\ndecision.\n\nEvery value here is interpreted ONLY relative to `scheme`, which is why `scheme` is the one\nrequired field: two markings from different schemes are not comparable, and a classification\nwith no scheme beside it is a string somebody will compare anyway.",
      "properties": {
        "caveats": {
          "description": "As that scheme spells them.",
          "items": {
            "type": "string"
          },
          "title": "Caveats",
          "type": "array"
        },
        "classification": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "As that scheme spells it. Transported verbatim.",
          "title": "Classification"
        },
        "marking_extras": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Scheme-specific values with no generic counterpart. Strings, because dimension B checks that marking values are strings and because the block has exactly one generic open bag and it is oes.extensions.",
          "title": "Marking Extras",
          "type": "object"
        },
        "originator": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "As that scheme identifies them.",
          "title": "Originator"
        },
        "releasability": {
          "description": "As that scheme spells them. Never reordered.",
          "items": {
            "type": "string"
          },
          "title": "Releasability",
          "type": "array"
        },
        "scheme": {
          "description": "Which marking system these values belong to. Required.",
          "minLength": 1,
          "title": "Scheme",
          "type": "string"
        }
      },
      "required": [
        "scheme"
      ],
      "title": "SecurityMarking",
      "type": "object"
    },
    "Severity": {
      "enum": [
        "INFO",
        "ADVISORY",
        "WARNING",
        "CRITICAL"
      ],
      "title": "Severity",
      "type": "string"
    },
    "SourceId": {
      "additionalProperties": false,
      "description": "One external identifier for an object \u2014 the provenance mapping.\n\nA list of these, not one, because the same object arrives from several systems: the same\nvessel is an MMSI to AIS, a track number to STANAG 4676 and a UID to TAK. Fusion joins\nthem later; the adapter's job is to record which name its own system used, and never to\noverwrite another system's entry.",
      "properties": {
        "external_id": {
          "description": "That system's own identifier.",
          "minLength": 1,
          "title": "External Id",
          "type": "string"
        },
        "system": {
          "description": "The external system, e.g. PNTMAP, TAK.",
          "minLength": 1,
          "title": "System",
          "type": "string"
        }
      },
      "required": [
        "system",
        "external_id"
      ],
      "title": "SourceId",
      "type": "object"
    },
    "SourceRef": {
      "additionalProperties": false,
      "description": "Which adapter produced this object, from which system, and whether it is real.\n\n`synthetic` is required and has no default. Every fixture in this repository is synthetic\nand every scenario package is too (TR-12), and the platform keeps the synthetic and live\nlayers apart over one interface \u2014 so an object that does not say which layer it belongs to\ncannot be filed. A default of `false` would silently promote exercise data to operational\ndata, which is the dangerous direction; a default of `true` would silently demote live\ndata and hide it from an operator. There is no safe default, so there is no default.",
      "properties": {
        "adapter": {
          "description": "Adapter name, e.g. pntmap.",
          "minLength": 1,
          "title": "Adapter",
          "type": "string"
        },
        "adapter_version": {
          "description": "Adapter semver.",
          "minLength": 1,
          "title": "Adapter Version",
          "type": "string"
        },
        "synthetic": {
          "description": "true for anything not from a real source (TR-12).",
          "title": "Synthetic",
          "type": "boolean"
        },
        "system": {
          "description": "The external system this came from.",
          "minLength": 1,
          "title": "System",
          "type": "string"
        }
      },
      "required": [
        "system",
        "adapter",
        "adapter_version",
        "synthetic"
      ],
      "title": "SourceRef",
      "type": "object"
    },
    "Track": {
      "additionalProperties": false,
      "description": "An entity's position history, in time order. The order is a contract, not a hope.\n\nA scrambled sample list produces nonsense the moment anything differentiates it \u2014 speed\nfrom consecutive positions, a heading arrow, a predicted point. So non-decreasing\ntimestamps are validated here, at the boundary, where the defect is one adapter's bug\nrather than a mystery in a fusion layer three hops downstream.\n\nEqual timestamps are ALLOWED: two sensors reporting the same instant is real, and\nrejecting it would refuse legitimate multi-source data.",
      "properties": {
        "entity_id": {
          "description": "The Entity this history belongs to.",
          "format": "uuid",
          "title": "Entity Id",
          "type": "string"
        },
        "integrity": {
          "anyOf": [
            {
              "$ref": "#/$defs/Integrity"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "PQC signature block \u2014 designed, not yet populated."
        },
        "object_kind": {
          "const": "track",
          "default": "track",
          "title": "Object Kind",
          "type": "string"
        },
        "samples": {
          "description": "Time-ordered, non-decreasing. At least one.",
          "items": {
            "$ref": "#/$defs/TrackSample"
          },
          "minItems": 1,
          "title": "Samples",
          "type": "array"
        },
        "schema_version": {
          "default": "2.0.0",
          "description": "Semver of the CDM this object was written against.",
          "title": "Schema Version",
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/SourceRef",
          "description": "Which adapter produced this object. Required on every kind."
        },
        "source_ids": {
          "description": "Every external identifier this object is known by. At least one, on EVERY kind \u2014 see the class docstring.",
          "items": {
            "$ref": "#/$defs/SourceId"
          },
          "minItems": 1,
          "title": "Source Ids",
          "type": "array"
        },
        "track_id": {
          "format": "uuid",
          "title": "Track Id",
          "type": "string"
        },
        "track_quality": {
          "anyOf": [
            {
              "maximum": 1.0,
              "minimum": 0.0,
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "0..1. None = not assessed, never 0.",
          "title": "Track Quality"
        }
      },
      "required": [
        "source",
        "source_ids",
        "track_id",
        "entity_id",
        "samples"
      ],
      "title": "Track",
      "type": "object"
    },
    "TrackSample": {
      "additionalProperties": false,
      "description": "One position at one instant. The unit STANAG 4676 calls a track point.",
      "properties": {
        "observed_at": {
          "description": "RFC 3339 UTC, exactly three decimal places, always Z.",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$",
          "title": "Observed At",
          "type": "string"
        },
        "position": {
          "$ref": "#/$defs/Position"
        }
      },
      "required": [
        "position",
        "observed_at"
      ],
      "title": "TrackSample",
      "type": "object"
    },
    "Verification": {
      "description": "How corroborated the assertion is. NOT confidence \u2014 `06-verification-and-confidence.md`.\n\nThey vary independently and every combination is meaningful: a single high-grade sensor can\nbe highly confident and entirely uncorroborated, and three weak sources can corroborate each\nother and leave the producer unsure. Absence means nothing is asserted about corroboration;\n`UNVERIFIED` is the positive claim that the question was asked and the answer was \"no\", which\nis a different and more informative fact. `DISPUTED` records that a disagreement exists and\nis not a verdict on it.",
      "enum": [
        "UNVERIFIED",
        "CORROBORATED",
        "VALIDATED",
        "DISPUTED"
      ],
      "title": "Verification",
      "type": "string"
    }
  },
  "$id": "urn:synapsecommand:cdm:2.0.0:cdm_object",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "discriminator": {
    "mapping": {
      "entity": "#/$defs/Entity",
      "event": "#/$defs/Event",
      "plan_object": "#/$defs/PlanObject",
      "track": "#/$defs/Track"
    },
    "propertyName": "object_kind"
  },
  "oneOf": [
    {
      "$ref": "#/$defs/Entity"
    },
    {
      "$ref": "#/$defs/Event"
    },
    {
      "$ref": "#/$defs/Track"
    },
    {
      "$ref": "#/$defs/PlanObject"
    }
  ],
  "x-cdm-schema-version": "2.0.0"
}
